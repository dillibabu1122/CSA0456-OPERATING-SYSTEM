#include <stdio.h>
#include <unistd.h>
int main() {
    if (fork() == 0)
        printf("Child PID: %d, Parent PID: %d\n", getpid(), getppid());
    else
        printf("Parent PID: %d\n", getpid());

    return 0;
}

OUTPUT:
Parent PID: 36452
Child PID: 36453, Parent PID: 36452
